services:
  webserver:
    build:
      context: ./backend
      dockerfile: Dockerfile

  nginx:
    image: nginx:1.15
    ports:
      - 80:80
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro 
